# image_classifier
mage classification pipeline using a manually defined ResNet-50 architecture in Keras. Instead of relying solely on high-level wrappers, the model structure is explicitly constructed, demonstrating a strong understanding of deep learning architecture design.
